# 관심사 선택 화면의 기본 선택값 제거

- **ID**: 005
- **날짜**: 2026-07-18
- **리뷰 ID**: wstxrzchrxhdhjgnqcjzsrlidleyisuu
- **유형**: UX 개선

## 작업 요약

가입 흐름의 관심사 단계에서 산책·사진·맛집이 미리 선택되던 기본값을 제거했다.
사용자는 관심사 화면에 처음 진입할 때 0/6 선택 상태에서 직접 관심사를 고르게 된다.

## 원문 요청사항

```text
관심사 들어갈 때 아무것도 선택 안된 상태면 좋을 거 같아.
```

## 변경 파일 목록

- `src/app/page.home/view.ts` — `selectedInterests` 초기값을 빈 배열로 변경
- `devlog.md` — 작업 요약 행 추가
- `devlog/2026-07-18/005-empty-interest-selection.md` — 상세 작업 기록 추가

## 검증 결과

- WIZ 일반 빌드(`clean=false`): 성공, EsBuild 오류 없음
- 관심사 초기값이 `[]`로 설정된 것 확인
- 기존 최소 3개 선택 검증과 최대 6개 선택 제한 유지 확인
- `git diff --check`: 통과

## 남은 리스크

- 이미 같은 화면 세션에서 선택한 관심사는 이전 단계로 돌아갔다가 재진입하면 유지된다. 이번 변경은 가입 흐름 최초 진입의 기본 상태만 비운다.
