# 3D 캐릭터 및 장소 추천 수정 내역

## 적용 범위

- 기존 Phaser 2D 월드맵은 그대로 유지했습니다.
- 월드맵을 GLB/Three.js로 교체했던 코드는 포함하지 않았습니다.
- 충녕이 GLB 캐릭터의 방향, 이동, 애니메이션과 원격 사용자 동기화를 개선했습니다.
- 대화 기반 장소 추천의 의도 판별과 결과 품질을 개선했습니다.

## 3D 캐릭터

- `chungnyeong_idle.glb`, `chungnyeong_walk.glb`, `chungnyeong_run.glb`를 실제 상태에 맞춰 사용합니다.
- 게임 시작 전에 세 모델을 미리 로드해 첫 이동 때 애니메이션이 늦게 나타나는 현상을 줄였습니다.
- WASD/방향키 입력을 정규화해 대각선 이동 속도가 더 빨라지지 않게 했습니다.
- 이동 벡터에서 목표 yaw를 계산하고 최단 각도로 부드럽게 회전합니다.
- 좌우 이동 방향과 모델 정면 보정을 분리했습니다.
- Shift와 이동키를 함께 누르면 달리기 속도와 run 모델이 적용됩니다.
- 프레임 기반 이동을 delta time 기반 이동으로 변경하고 큰 delta를 제한했습니다.
- 로컬 사용자의 yaw와 `idle | walk | run` 상태를 Socket.IO로 제한된 주기로 전송합니다.
- 원격 캐릭터도 위치와 회전을 보간하고 같은 애니메이션 규칙을 사용합니다.
- 개발 환경에서 `/dev/character-test` 및 캐릭터 디버그 패널로 방향과 설정을 확인할 수 있습니다.

## 장소 추천 답변

- 대화에 음식점, 카페, 보드게임, 영화, 산책, 전시 등 구체적인 활동이 없으면 추천을 실행하지 않습니다.
- 사용자 프로필만으로 카페를 임의 선택하던 기본값을 제거했습니다.
- 보드게임 카페를 일반 카페와 분리해 보드게임 카페를 먼저 검색하고 부족할 때만 일반 카페로 보충합니다.
- `조용한`, `차분한`, `한적한`, `디저트`, `분위기 좋은` 같은 표현을 검색어와 점수에 반영합니다.
- 보험사, 은행, 금융사, 병원, 부동산, 사무소 같은 비데이트 업종을 공통 제외합니다.
- 음식점과 카페뿐 아니라 볼링, 방탈출, 전시, 공연 같은 여가 의도를 지원합니다.
- 최근 30분 동안 같은 채팅방에 추천한 장소에 감점을 적용해 동일한 세 곳의 반복을 줄였습니다.
- 유효한 활동이 없는 요청은 추천 쿨다운을 소비하지 않습니다.

## 확인 명령

```bash
npm run inspect:character
npm run test:character
npm run build
```

개발 서버 실행 후 `/dev/character-test`에서 WASD, 대각선 이동, Shift 달리기와 방향 화살표를 확인할 수 있습니다.
